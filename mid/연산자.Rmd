# 수업 복습: 데이터 시각화

이 문서는 수업에서 배운 R ggplot2 패키지를 복습하기 위해 작성되었습니다.

```{r}
# ggplot2 패키지 로드
library(ggplot2)

# 예시 데이터 생성
df <- data.frame(
  x = 1:10,
  y = 1:10 + rnorm(10)
)

# 산점도 그리기
ggplot(df, aes(x = x, y = y)) +
  geom_point() +
  labs(title = "복습용 산점도")
